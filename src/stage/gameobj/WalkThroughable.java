package stage.gameobj;

public interface WalkThroughable extends IDrawable{
	/**
	 * This interface represent the object which is can be walkThrough.
	 * @return
	 */
	public boolean isWalkThroughable();
}
